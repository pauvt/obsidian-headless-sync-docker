services:
  obsidian-sync:
    image: ghcr.io/pauvt/obsidian-headless-sync-docker:latest
    container_name: obsidian-sync
    environment:
      # Required
      OBSIDIAN_AUTH_TOKEN: ${OBSIDIAN_AUTH_TOKEN}
      VAULT_NAME: ${VAULT_NAME}
      # Required if vault uses end-to-end encryption
      VAULT_PASSWORD: ${VAULT_PASSWORD:-}
      # File ownership: UID:GID of the host user who should own vault files.
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      # Optional overrides
      VAULT_PATH: ${VAULT_PATH:-/vault}
      DEVICE_NAME: ${DEVICE_NAME:-obsidian-docker}
      CONFLICT_STRATEGY: ${CONFLICT_STRATEGY:-merge}
      EXCLUDED_FOLDERS: ${EXCLUDED_FOLDERS:-}
      FILE_TYPES: ${FILE_TYPES:-}
      SYNC_MODE: ${SYNC_MODE:-}
      SYNC_CONFIGS: ${SYNC_CONFIGS:-}
    volumes:
      - ${VAULT_HOST_PATH:-./vault}:/vault
      - ${CONFIG_HOST_PATH:-./config}:/home/obsidian/.config
    restart: unless-stopped
